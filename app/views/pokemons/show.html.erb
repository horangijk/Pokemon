<h1>This is <%= @pokemon.name %>'s Page: </h1>

<h3>These are all of its Trainers:</h3>

<ul>
  <% @pokemon.users.each do |user| %>
    <li><%= link_to user.username, user %></li>
  <% end %>
</ul>

<h3>These are all of <%= @pokemon.name %>'s moves:</h3>
<br>
<ul>
  <% @pokemon.moves.each do |move| %>
  <li><%= move.name %></li>
  <% end %>
</ul>
